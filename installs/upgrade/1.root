#!/bin/bash -eu

log "apt-get ^update^"
apt-get --yes update

log "apt-get ^dist-upgrade^"
apt-get --yes dist-upgrade

if [ -n "$(command -v apt-file)" ]; then
    log "apt-file ^update^"
    apt-file update
fi
